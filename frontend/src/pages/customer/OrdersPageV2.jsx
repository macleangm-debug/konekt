import React, { useState, useEffect } from "react";
import { Link } from "react-router-dom";
import { Package, X } from "lucide-react";
import PageHeader from "../../components/ui/PageHeader";
import SurfaceCard from "../../components/ui/SurfaceCard";
import FilterBar from "../../components/ui/FilterBar";
import BrandButton from "../../components/ui/BrandButton";
import axios from "axios";
const API_URL = process.env.REACT_APP_BACKEND_URL || "";
function money(v){ return `TZS ${Number(v||0).toLocaleString()}`; }
function fmtDate(v){ try { return new Date(v).toLocaleDateString('en-GB',{day:'2-digit',month:'short',year:'numeric'});} catch { return '-'; } }
const STATUS_COLORS = { pending_payment_confirmation:'bg-amber-100 text-amber-700', processing:'bg-blue-100 text-blue-700', ready_to_fulfill:'bg-purple-100 text-purple-700', completed:'bg-green-100 text-green-700', cancelled:'bg-red-100 text-red-700' };
export default function OrdersPageV2(){ const [orders,setOrders]=useState([]); const [loading,setLoading]=useState(true); const [searchValue,setSearchValue]=useState(''); const [statusFilter,setStatusFilter]=useState(''); const [selected,setSelected]=useState(null);
 useEffect(()=>{ const token=localStorage.getItem('token'); if(!token) return; axios.get(`${API_URL}/api/customer/orders`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>setOrders(r.data||[])).catch(e=>console.error(e)).finally(()=>setLoading(false)); },[]);
 const filtered = orders.filter(order=>{ const ref=`${order.order_number||''} ${order.id||''}`.toLowerCase(); return (!searchValue || ref.includes(searchValue.toLowerCase())) && (!statusFilter || order.status===statusFilter || order.current_status===statusFilter); });
 return <div className="space-y-6" data-testid="orders-page"><PageHeader title="Orders" subtitle="Track fulfillment after payment approval." actions={<BrandButton href="/marketplace" variant="primary">New Order</BrandButton>} /><FilterBar searchValue={searchValue} onSearchChange={setSearchValue} searchPlaceholder="Search orders..." filters={[{name:'status',value:statusFilter,onChange:setStatusFilter,placeholder:'All Statuses',options:[{value:'pending_payment_confirmation',label:'Awaiting Payment'},{value:'processing',label:'Processing'},{value:'ready_to_fulfill',label:'Ready to Fulfill'},{value:'completed',label:'Completed'}]}]} />
 <div className="grid xl:grid-cols-[1fr_380px] gap-6"><SurfaceCard className="overflow-hidden p-0"><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="px-4 py-3 text-left font-semibold">Date</th><th className="px-4 py-3 text-left font-semibold">Order</th><th className="px-4 py-3 text-left font-semibold">Type</th><th className="px-4 py-3 text-left font-semibold">Amount</th><th className="px-4 py-3 text-left font-semibold">State</th><th className="px-4 py-3 text-right font-semibold">Actions</th></tr></thead><tbody>{loading ? <tr><td colSpan="6" className="px-4 py-10 text-center text-slate-500">Loading orders...</td></tr> : filtered.length ? filtered.map(order=>{ const state=order.current_status||order.status||'processing'; return <tr key={order.id||order._id} className={`border-t hover:bg-slate-50 ${selected?.id===(order.id||order._id)?'bg-[#20364D]/5':''}`}><td className="px-4 py-4 text-slate-600 whitespace-nowrap">{fmtDate(order.created_at)}</td><td className="px-4 py-4"><div className="font-semibold text-[#20364D]">{order.order_number||order.id?.slice(-8)}</div><div className="text-xs text-slate-500">{(order.items||[]).length} item(s)</div></td><td className="px-4 py-4 text-slate-600">{(order.type||'order').replace(/_/g,' ')}</td><td className="px-4 py-4 font-semibold text-[#20364D]">{money(order.total||order.total_amount)}</td><td className="px-4 py-4"><span className={`px-3 py-1 rounded-full text-xs font-semibold ${STATUS_COLORS[state]||'bg-slate-100 text-slate-700'}`}>{state.replace(/_/g,' ')}</span></td><td className="px-4 py-4"><div className="flex justify-end gap-2"><button onClick={()=>setSelected(order)} className="rounded-xl border px-3 py-2 hover:bg-slate-50">View</button><Link to={`/dashboard/orders/${order.id||order._id}`} className="rounded-xl border px-3 py-2 hover:bg-slate-50">Open</Link></div></td></tr>; }) : <tr><td colSpan="6" className="px-4 py-12 text-center text-slate-500"><Package className="w-12 h-12 mx-auto mb-3 text-slate-300"/><div className="font-semibold text-[#20364D] mb-1">No orders yet</div><div>Paid orders will appear here automatically.</div></td></tr>}</tbody></table></div></SurfaceCard><SurfaceCard>{selected ? <div className="space-y-4"><div className="flex items-center justify-between"><div><div className="text-xl font-bold text-[#20364D]">{selected.order_number||selected.id?.slice(-8)}</div><div className="text-sm text-slate-500">{fmtDate(selected.created_at)}</div></div><button onClick={()=>setSelected(null)} className="p-2 rounded-lg hover:bg-slate-100"><X className="w-4 h-4"/></button></div><div className="grid grid-cols-2 gap-3 text-sm"><div><div className="text-slate-500">Status</div><div className="font-semibold text-[#20364D]">{(selected.current_status||selected.status||'').replace(/_/g,' ')}</div></div><div><div className="text-slate-500">Payment</div><div className="font-semibold text-[#20364D]">{selected.payment_status||'paid'}</div></div><div><div className="text-slate-500">Type</div><div className="font-semibold text-[#20364D]">{selected.type||'-'}</div></div><div><div className="text-slate-500">Amount</div><div className="font-semibold text-[#20364D]">{money(selected.total||selected.total_amount)}</div></div></div><div><div className="text-sm font-semibold text-slate-500 mb-2">Items</div><div className="space-y-2">{(selected.items||[]).map((item,idx)=><div key={idx} className="flex items-center justify-between text-sm bg-slate-50 rounded-xl px-3 py-2"><span>{item.name} x{item.quantity||1}</span><span className="font-semibold">{money(item.line_total||item.unit_price||item.price)}</span></div>)}</div></div></div> : <div className="text-center py-16 text-slate-400">Select an order to see more details</div>}</SurfaceCard></div></div>; }
